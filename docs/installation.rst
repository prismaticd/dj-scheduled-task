============
Installation
============

At the command line::

    $ easy_install dj-scheduled-task

Or, if you have virtualenvwrapper installed::

    $ mkvirtualenv dj-scheduled-task
    $ pip install dj-scheduled-task
